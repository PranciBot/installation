helm upgrade -i infra-storage /mnt/e/Dev/Project/personal-assistant/infra-storage/deploy/chart/ --set persistent.hostpath=/home/dthvinh/local/mount/personal-asssitant

helm upgrade -i infra /mnt/e/Dev/Project/personal-assistant/infra/deploy/chart/ \
    --set persistent-storage.primary.persistence.existingClaim=infra-storage-postgres-pvc \
    --set event-dispatcher.kafka.externalAdvertisedHost=localhost \
    --set platform-gateway.gateway.proxy.type=LoadBalancer

helm upgrade -i apps /mnt/e/Dev/Project/personal-assistant/apps/deploy/chart/ \
    --set global.infraName=infra \
    --set chat-server.image.group=$(whoami) \
    --set lifestyle-service.image.group=$(whoami)
